[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-ai-roi-platform"
version = "0.1.0"
description = "Enterprise AI Use-Case and ROI Assessment Platform"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]